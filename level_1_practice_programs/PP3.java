package level_1_practice_programs;

class DistanceConverter {
    public static void main(String[] args) {
        double dist_kms = 10.8;
        double dist_miles = dist_kms * 1.6;

        System.out.println("The distance " + dist_kms + "km in miles is " + dist_miles);
    }
}
